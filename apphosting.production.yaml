# Production configuration for 10-50 daily users
runtime: nodejs20

# F2 minimum for production builds (F1 causes memory errors during build)
instance_class: F2  # 1.2GHz CPU, 256MB RAM - required for successful builds
service: default

automatic_scaling:
  min_instances: 0        # Scale to zero during off-peak to minimize costs
  max_instances: 3        # Sufficient for 10-50 daily users
  target_cpu_utilization: 0.70  # Higher threshold for cost optimization
  target_throughput_utilization: 0.80
  max_concurrent_requests: 80    # Adequate for small user base

handlers:
  # Static assets with aggressive caching
  - url: /(.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp|avif))
    static_files: build/\1
    upload: build/(.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp|avif))
    secure: always
    http_headers:
      Cache-Control: "public, max-age=31536000, immutable"
      Vary: "Accept-Encoding"
    expiration: "365d"

  # HTML files with shorter cache
  - url: /(.*\.html)$
    static_files: build/\1
    upload: build/(.*\.html)$
    secure: always
    http_headers:
      Cache-Control: "public, max-age=3600, must-revalidate"
      Vary: "Accept-Encoding"
    expiration: "1h"

  # Root and other routes
  - url: /(.*)
    static_files: build/index.html
    upload: build/index.html
    secure: always
    redirect_http_response_code: 301
    http_headers:
      Cache-Control: "public, max-age=3600, must-revalidate"
      Vary: "Accept-Encoding"
      X-Content-Type-Options: "nosniff"
      X-Frame-Options: "DENY"
      X-XSS-Protection: "1; mode=block"
      Referrer-Policy: "strict-origin-when-cross-origin"

env:
  # Optimized Node.js settings for F2 instances and build process
  - variable: NODE_OPTIONS
    value: "--max-old-space-size=1024"  # Proper memory allocation for F2 (256MB RAM)

  - variable: VITE_SUPABASE_URL
    secret: supabaseUrlSecretProd
    availability:
      - BUILD
      - RUNTIME
    
  - variable: VITE_SUPABASE_ANON_KEY
    secret: supabaseAnonKeySecretProd
    availability:
      - BUILD
      - RUNTIME

  - variable: VITE_STRIPE_PUBLISHABLE_KEY
    secret: stripePK_PROD
    availability:
      - BUILD
      - RUNTIME

  - variable: VITE_STRIPE_SECRET_KEY
    secret: stripeSK_PROD
    availability:
      - BUILD
      - RUNTIME

# Performance and reliability settings
inbound_services:
  - warmup  # Keeps instances warm to reduce cold starts

# Optimized timeout for static content (was 300s - way too high!)
timeout: 30s  # Sufficient for static files, forces optimization

# Health checks for better reliability
readiness_check:
  path: "/"
  check_interval_sec: 5
  timeout_sec: 4
  failure_threshold: 2
  success_threshold: 2
  app_start_timeout_sec: 300

liveness_check:
  path: "/"
  check_interval_sec: 30
  timeout_sec: 4
  failure_threshold: 4
  success_threshold: 2

# Security and performance headers
network:
  forwarded_ports:
    - 443
  session_affinity: false  # Not needed for static content

# Note: Grant access to secrets using:
# firebase apphosting:secrets:grantaccess stripeSK_PROD --backend prod-paw-fi-web
# firebase apphosting:secrets:grantaccess supabaseUrlSecretProd --backend prod-paw-fi-web
# firebase apphosting:secrets:grantaccess supabaseAnonKeySecretProd --backend prod-paw-fi-web
# firebase apphosting:secrets:grantaccess stripePK_PROD --backend prod-paw-fi-web