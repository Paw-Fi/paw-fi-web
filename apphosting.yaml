# Firebase App Hosting configuration using modern scripts format
runtime: nodejs22

scripts:
  buildCommand: rm -f package-lock.json && npm install && npm run build
  runCommand: npm run start

outputFiles:
  serverApp:
    include: [.output]